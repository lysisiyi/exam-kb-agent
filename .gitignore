# ─────────────────────────────────────────────────────────────────────────────
# 考研数学错题 Agent —— 版本控制忽略规则
#
# 原则：**只提交"事实源"与"手写的东西"，不提交任何可再生的派生物。**
# 判断标准：删掉它之后，跑一条命令就能原样恢复的，就该忽略。
# ─────────────────────────────────────────────────────────────────────────────

# ── Flutter / Dart（全部可再生） ────────────────────────────────────────────
app/build/
app/.dart_tool/
app/.flutter-plugins
app/.flutter-plugins-dependencies
app/.pub-cache/
app/.pub/

# Flutter 为各平台生成的临时目录（含 270 MB 的 flutter_windows.dll.pdb）
app/windows/flutter/ephemeral/
app/linux/flutter/ephemeral/
app/macos/Flutter/ephemeral/

# ── 本项目的派生物 ──────────────────────────────────────────────────────────
#
# data/ → app/assets/data/ 是 tools/data/sync_assets.py 生成的**消费副本**。
# 事实源是 data/；Flutter 不允许 pubspec 引用应用目录外的路径，才需要这一份。
# 克隆后跑一次 `python tools/data/sync_assets.py` 即可（run_app.bat 也会跑）。
app/assets/data/

# ── 日志与临时文件 ──────────────────────────────────────────────────────────
*.log
*.tmp
*.swp

# ── Python ──────────────────────────────────────────────────────────────────
__pycache__/
*.pyc

# ─────────────────────────────────────────────────────────────────────────────
# ⚠️ 以下两个目录**不属于本项目**，只是恰好放在同一个工作目录下。
#    忽略它们不是因为它们是"派生物"，而是因为它们各有归属。
# ─────────────────────────────────────────────────────────────────────────────

# 星匣AiGameJam 是另一个独立的 npm/TypeScript 项目（291 MB / 4333 文件，
# 含 node_modules、.npm-cache、recordings/*.mp4）。
# 如果它需要版本控制，应该在**它自己的目录里** git init，而不是并进这个仓库。
星匣AiGameJam/

# .perf 是 DSH 的性能测试脚手架（22 MB：ab-*.ps1 / prof / dsh-home），
# 与本项目无关，属于工具链的临时产物。
.perf/

# ── 编辑器与系统 ────────────────────────────────────────────────────────────
.idea/
.vscode/
*.iml
*.ipr
*.iws
.DS_Store
Thumbs.db
desktop.ini
