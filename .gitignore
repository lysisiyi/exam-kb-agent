# ─────────────────────────────────────────────────────────────────────────────
# 考试知识库 Agent（exam-kb-agent）—— 版本控制忽略规则
#
# 原则：**只提交"事实源"与"手写的东西"，不提交任何可再生的派生物。**
# 判断标准：删掉它之后，跑一条命令就能原样恢复的，就该忽略。
# ─────────────────────────────────────────────────────────────────────────────

# ── Flutter / Dart（全部可再生） ────────────────────────────────────────────
app/build/
app/.dart_tool/
app/.flutter-plugins
app/.flutter-plugins-dependencies
# `flutter` 命令在**运行目录**留下的工具状态缓存（可再生）
#
# ⚠️ 不能只写 `app/.flutter_tool_state`：在仓库根目录跑一次
# `flutter analyze`（例如配合 `env -u HTTP_PROXY` 那套环境变量）就会在
# 根上生成一份，而它记录的是**这台机器**的设置（`{"is-bot": false}`）——
# 提交上去会让别人的 flutter 读到我的状态。所以用不带路径的写法。
.flutter_tool_state
app/.pub-cache/
app/.pub/

# Flutter 为各平台生成的临时目录（含 270 MB 的 flutter_windows.dll.pdb）
app/windows/flutter/ephemeral/
app/linux/flutter/ephemeral/
app/macos/Flutter/ephemeral/

# ── 本项目的派生物 ──────────────────────────────────────────────────────────
#
# data/ → app/assets/data/ 是 tools/data/sync_assets.py 生成的**消费副本**。
# 事实源是 data/；Flutter 不允许 pubspec 引用应用目录外的路径，才需要这一份。
# 克隆后跑一次 `python tools/data/sync_assets.py` 即可（run_app.bat 也会跑）。
app/assets/data/

# ── 日志与临时文件 ──────────────────────────────────────────────────────────
*.log
*.tmp
*.swp

# ── Python ──────────────────────────────────────────────────────────────────
__pycache__/
*.pyc

# ── 编辑器与系统 ────────────────────────────────────────────────────────────
.idea/
.vscode/
*.iml
*.ipr
*.iws
.DS_Store
Thumbs.db
desktop.ini

# 说明：本仓库是**自足的** —— 它只包含自己需要的东西，不依赖任何同级的兄弟目录。
# 早先这里写过两条针对工作目录里其他项目的忽略规则，那其实是无效的
# （`.gitignore` 管不到仓库目录之外），已删除。

